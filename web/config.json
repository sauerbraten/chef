{
	// SQlite database file name
	"db_file_path": "/home/axle/.chef/chef.sqlite",

	// web interface address
	// must be a publicly reachable address, since the IRC bot includes links to it in its replies
	"web_interface_address": "localhost:6060"
}